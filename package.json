{
  "name": "coffee_app_project",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "cd frontend && npm start",
    "start-api": "cd api && venv/bin/flask run --no-debugger",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test"
  }
} 